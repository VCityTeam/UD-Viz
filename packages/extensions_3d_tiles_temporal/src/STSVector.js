import { STShape } from './STShape';

export class STSVector extends STShape {
  constructor(stLayer, options = {}) {
    super(stLayer);
  }
  display() {
    super.display();
  }

  update() {}

  dispose() {
    super.dispose();
  }
}
